<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.andersen.rickandmorty">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RickAndMorty">
        <activity android:name=".ui.SplashScreenActivity"></activity>
        <activity
            android:name=".ui.locations.LocationActivity"
            android:parentActivityName=".ui.MainActivity" />
        <activity
            android:name=".ui.episodes.EpisodeActivity"
            android:parentActivityName=".ui.MainActivity" />
        <activity
            android:name=".ui.characters.CharacterActivity"
            android:parentActivityName=".ui.MainActivity" />
        <activity
            android:name=".ui.MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>